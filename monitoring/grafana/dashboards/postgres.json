{
  "annotations": {"list": []},
  "editable": true,
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "fieldConfig": {"defaults": {"unit": "short"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "targets": [{"expr": "pg_stat_database_numbackends", "legendFormat": "{{datname}}"}],
      "title": "PostgreSQL connections",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "fieldConfig": {"defaults": {"unit": "ops"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "targets": [{"expr": "rate(pg_stat_database_xact_commit[5m])", "legendFormat": "commits {{datname}}"}, {"expr": "rate(pg_stat_database_xact_rollback[5m])", "legendFormat": "rollbacks {{datname}}"}],
      "title": "Transactions",
      "type": "timeseries"
    }
  ],
  "refresh": "15s",
  "schemaVersion": 39,
  "tags": ["postgres", "sebook"],
  "time": {"from": "now-6h", "to": "now"},
  "timezone": "browser",
  "title": "SEBook PostgreSQL",
  "uid": "sebook-postgres",
  "version": 1
}
